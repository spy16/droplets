// Package stores contains different store implementations using MongoDB
// as storage backend. If multiple storage backends are added, these files
// could be moved into appropriate packages (mongo, psql etc.) based on the
// backend they are using.
package stores
